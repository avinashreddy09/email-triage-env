[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "email-triage-agent"
version = "1.0.0"
description = "Email Triage Environment"
dependencies = ["openenv-core>=0.2.0"]

[project.scripts]
email-triage-agent = "server.app:main"
